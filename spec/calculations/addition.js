import {add, subtract} from '../src/Utilities/calculations.mjs'
import {DimensionError} from "../src/Utilities/errors.mjs";

describe("Addition", function() {

    it("where an element is '2a' added to element '1a''2a + 1a' should return '3a' in given result element.", function () {

    });

    it("where an element is '2a' added to element '1 + a' should return '3a + 1' in given result element.", function () {

    });
});